# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

before:
  hooks:
    - go mod tidy

builds:
  - env:
      - CGO_ENABLED=0
    main: ./src
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    # use zip for windows archives
    format_overrides:
      - goos: windows
        formats: [zip]

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

binary_signs:
  - {}

signs:
  - artifacts: checksum

winget:
  - name: anonymongo
    publisher: Yuval Herziger <yuvalhrz@gmail.com>
    short_description: A CLI tool to redact sensitive values from MongoDB log files
    license: mit
    license_url: https://github.com/yuvalherziger/anonymongo/blob/main/LICENSE
    publisher_url: https://github.com/yuvalherziger/anonymongo
    publisher_support_url: https://github.com/yuvalherziger/anonymongo/issues/new
    repository: https://github.com/yuvalherziger/anonymongo




homebrew_casks:
  -
    name: anonymongo
    repository:
      owner: yuvalherziger
      name: homebrew-anonymongo
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    description: "A CLI tool to redact sensitive values from MongoDB log files"
    homepage: "https://github.com/yuvalherziger/homebrew"
    license: "MIT"
    caveats: "To test the installation, run: anonymongo version"
